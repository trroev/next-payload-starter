# MongoDB Atlas Setup Runbook

This runbook reproduces the provisioning of the Mise MongoDB Atlas cluster (RECIPE-066). Local development uses the Docker container from `docker-compose.yml` by default; Atlas backs PR preview deploys (RECIPE-047) and the production deploy (RECIPE-046).

## Topology

| | Cluster | Database | DB User | Used by |
|---|---|---|---|---|
| Staging | `Cluster0` (M0) | `<project>_staging` | `<project>_staging_user` | PR preview deploys, optional local opt-in |
| Production | `Cluster0` (M0) | `<project>_production` | `<project>_production_user` | Production Vercel deploy |

Staging and production share one cluster (M0 free tier) but live in separate databases, each with its own scoped DB user. A misconfigured staging connection string therefore cannot read or write production data.

## Provisioning steps

### 1. Project and cluster

1. Sign in to [cloud.mongodb.com](https://cloud.mongodb.com).
2. Create a new project named `<project>` under the `Trevor's Org` organization.
3. **Build a Database** → **M0 (Free)**.
   - Provider: AWS
   - Region: `us-east-1` (matches Vercel's default `iad1` Functions region)
   - Cluster name: `Cluster0`
4. Skip the user-creation prompt that appears after the cluster spins up — DB users are created manually below so each is scoped to a single database.

### 2. Databases

On the cluster's **Browse Collections** page, click **+ Create database** twice:

| Database name | Initial collection |
|---|---|
| `<project>_staging` | `_init` (placeholder; Payload creates real collections on first connect) |
| `<project>_production` | `_init` |

The `_init` collections can be dropped after Payload populates the real collections.

### 3. Database users

**Database Access** → **Add New Database User**, once per environment:

| Username | Privilege | Database | Password |
|---|---|---|---|
| `<project>_staging_user` | `readWrite` (Specific Privileges) | `<project>_staging` | autogenerated, copied to 1Password |
| `<project>_production_user` | `readWrite` (Specific Privileges) | `<project>_production` | autogenerated, copied to 1Password |

Both passwords are stored in 1Password under entries named **Mise — MongoDB Atlas (staging)** and **Mise — MongoDB Atlas (production)**.

### 4. Network access

**Network Access** → **Add IP Address** → `0.0.0.0/0`.

The allowlist is fully open because Vercel does not publish stable egress IPs on the Hobby/Pro plans. Authentication is via the per-database scoped users with strong autogenerated passwords. If the project ever moves to Vercel Enterprise (Secure Compute), tighten this to the published Vercel egress range.

### 5. Connection strings

Format:

```
mongodb+srv://<user>:<password>@cluster0.<id>.mongodb.net/<database>?retryWrites=true&w=majority&appName=Cluster0
```

Both URIs are stored in 1Password alongside their passwords, and are written into the encrypted `apps/web/.env.development` (staging) and `apps/web/.env.production` (production) via dotenvx:

```sh
pnpm dotenvx set MONGODB_URI '<staging-uri>' -f apps/web/.env.development
pnpm dotenvx set MONGODB_URI '<production-uri>' -f apps/web/.env.production
```

**The database segment is required.** Atlas's "Connect → Drivers" UI gives you a URI ending in `.mongodb.net/?...` (no DB). Without a DB in the path, the driver targets `test`, where the scoped users have no permissions — connections succeed but every query fails with `user is not allowed to do action [find] on [test.<collection>]`.

## Pointing local dev at staging Atlas

Default local development uses the Docker container at `mongodb://localhost:27017/<project>`. To exercise the staging Atlas database from a local checkout:

1. Get the staging URI from 1Password.
2. Replace `MONGODB_URI` in `.env.local` with the staging URI (`.env.local` overrides `.env.development`).
3. `pnpm dev`.

Production credentials must never be used from a local machine.

## Where credentials live

| Item | Location |
|---|---|
| Staging URI + password | 1Password: **Mise — MongoDB Atlas (staging)** |
| Production URI + password | 1Password: **Mise — MongoDB Atlas (production)** |
| Encrypted staging URI | `apps/web/.env.development` (decrypted via `.env.keys`) |
| Encrypted production URI | `apps/web/.env.production` (decrypted via `.env.keys`) |
| dotenvx private keys | `.env.keys` at repo root (gitignored; obtain from 1Password) |
