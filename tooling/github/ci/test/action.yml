name: 'Test'
description: 'Test'

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./tooling/github/setup

    - name: Configure Turborepo Caching
      uses: rharkor/caching-for-turbo@v2.3.11
      with:
        cache-prefix: test_

    - name: Test
      shell: bash
      run: pnpm test
